package ai.acyclic.prover.commons.graph

package object viz {

  /*
  at this moment, it is possible to have 2 duplicate nodes with different inductions.

  but Hierarchy visualisation may ignore this, as a result, the duplicated node may appears in 2 different rows

  Hasse diagram doesn't have this problem.
   */
}
